export module dx3d:core.common;
import std;
import :core.logger;

export namespace dx3d
{
	// The tutorial has the Desc types defined here,
	// but I've moved them to the relevant classes'
	// files to improve code locality.
}
